---
name: Application
route: /application
---

import { Playground, Props } from 'docz';
import { Paper, Typography, IconButton, Badge, List, ListItem, ListItemIcon, ListItemText } from '@material-ui/core';
import { Mail, Notifications, Inbox } from '@material-ui/icons';
import { ApplicationBar, UserMenu, DrawerMenu, RepositoryMenu, FileMenu } from './';
import { withAuthentication } from '../';

## ApplicationBar

The `ApplicationBar` component helps manage authentication via `UserMenu`.

It will render the component passed via `drawerMenu` prop to a drawer menu.

<Playground>
{() => {
  const [authentication, setAuthentication] = React.useState();
  const [repository, setRepository] = React.useState();
  const [blob, setBlob] = React.useState();
  const [file, setFile] = React.useState();
  const buttons = (
    <>
      <IconButton color="inherit">
        <Badge badgeContent={17} color="secondary">
          <Notifications />
        </Badge>
      </IconButton>
    </>
  );
  const drawerMenu = (
    <>
      <List>
        {['Inbox', 'Starred', 'Sent', 'Drafts'].map((text, index) => (
          <ListItem button key={text}>
            <ListItemIcon style={{margin:0}}>{index % 2 === 0 ? <Inbox /> : <Mail />}</ListItemIcon>
            <ListItemText primary={text} />
          </ListItem>
        ))}
      </List>
    </>
  );
  const config = {server: "https://git.door43.org"};
  return (
    <ApplicationBar
      title='Application Title'
      buttons={buttons}
      drawerMenu={drawerMenu}
      authentication={authentication}
      onAuthentication={setAuthentication}
      authenticationConfig={{
        tokenid:"PlaygroundTesting",
        ...config
      }}
      repository={repository}
      onRepository={setRepository}
      repositoryConfig={config}
      blob={blob}
      onBlob={setBlob}
      file={file}
      onFile={setFile}
    />
  );
}}
</Playground>

- Requires props from `withAuthentication` in addition to the below props.

<Props of={ApplicationBar} isToggle title="Props" />
<Props of={withAuthentication} isToggle title="Props" />

## UserMenu

The `UserMenu` component helps manage a authentication via a modal with a menu to login and logout.

<Playground>
{() => {
  const [authentication, setAuthentication] = React.useState();
  return (
    <Paper>
      <UserMenu
        authentication={authentication}
        onAuthentication={setAuthentication}
        authenticationConfig={{
          server: "https://git.door43.org",
          tokenid:"PlaygroundTesting",
        }}
      />
    </Paper>
  );
}}
</Playground>

- Requires props from `withAuthentication`.

<Props of={UserMenu} isToggle title="Props" />

## RepositoryMenu

The `RepositoryMenu` component helps manage a repository via a modal with a menu to select a repository.

<Playground>
{() => {
  const [repository, setRepository] = React.useState();
  return (
    <Paper>
      <RepositoryMenu
        repository={repository}
        onRepository={setRepository}
        repositoryConfig={{
          server: "https://git.door43.org",
        }}
      />
    </Paper>
  );
}}
</Playground>

- Requires props from `withAuthentication`.

<Props of={UserMenu} isToggle title="Props" />

## DrawerMenu

The `DrawerMenu` component helps display the `drawerMenu` component passed in props via a modal.

<Playground>
{() => {
  const drawerMenu = (
    <>
      <List>
        {['Inbox', 'Starred', 'Sent', 'Drafts'].map((text, index) => (
          <ListItem button key={text}>
            <ListItemIcon>{index % 2 === 0 ? <Inbox /> : <Mail />}</ListItemIcon>
            <ListItemText primary={text} />
          </ListItem>
        ))}
      </List>
    </>
  );
  return (
    <Paper>
      <DrawerMenu
        drawerMenu={drawerMenu}
      />
    </Paper>
  );
}}
</Playground>

<Props of={DrawerMenu} isToggle title="Props" />


## FileMenu

The `FileMenu` component helps select a File.

<Playground>
  <Paper>
    <FileMenu
      repositoryConfig={{
        server: "https://git.door43.org"
      }}
    />
  </Paper>
</Playground>

<Props of={FileMenu} isToggle title="Props" />
